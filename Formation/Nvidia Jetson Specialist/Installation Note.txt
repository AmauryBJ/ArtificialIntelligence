
#SD Card 
    Make a JetPack 4.5

#Make swapfile
    sudo fallocate -l 4G /var/swapfile
    sudo chmod 600 /var/swapfile
    sudo mkswap /var/swapfile
    sudo swapon /var/swapfile
    sudo bash -c 'echo "/var/swapfile swap swap defaults 0 0" >> /etc/fstab'

#Enable i2c permissions
    sudo usermod -aG i2c $USER

#install Python
    sudo apt-get update
    sudo apt install python3-pip python3-pil
    sudo pip3 install --upgrade numpy 

#install node
    sudo apt install nodejs npm

#upgrade node
    sudo npm cache clean -f
    sudo npm install -g n
    sudo n stable

#Dependencies for jupyter
    sudo apt install -y nodejs libffi-dev 

#Install Jupyter
    sudo pip3 install jupyter jupyterlab
    sudo jupyter labextension install @jupyter-widgets/jupyterlab-manager
    sudo jupyter labextension install @jupyterlab/statusbar
    jupyter lab --generate-config
    jupyter notebook password

    jupyter lab --generate-config
    #'amou' is the user name
    
    python3 -c "from notebook.auth.security import set_password; set_password('jetson', '/home/amou/.jupyter/jupyter_notebook_config.json')"

#Launch Jupyter
    jupyter notebook --ip=0.0.0.0 --no-browser

# install jetbot python module
    cd
    sudo apt install -y python3-smbus
    cd ~/jetbot
    sudo apt-get install -y cmake
    sudo python3 setup.py install 

# Install the pre-built PyTorch pip wheel 
    echo -e "\e[45m Install the pre-built PyTorch pip wheel  \e[0m"
    cd
    wget -N https://nvidia.box.com/shared/static/yr6sjswn25z7oankw8zy1roow9cy5ur1.whl -O torch-1.6.0rc2-cp36-cp36m-linux_aarch64.whl
    sudo apt-get install -y python3-pip libopenblas-base libopenmpi-dev 
    sudo -H pip3 install Cython
    sudo -H pip3 install numpy torch-1.6.0rc2-cp36-cp36m-linux_aarch64.whl 


#Jetcam 
    git clone https://github.com/NVIDIA-AI-IOT/jetcam
    cd jetcam
    sudo python3 setup.py install